/* シーン3（タイプ1／タイプ2／下パーツ生成）で共通のパネル注記。

   同じ文が3つのシーンに書かれていて、直すたびに3か所さわる形になっていた。
   ここへまとめて、主語（モデル／オブジェクト）だけ呼びもとから渡す。

   ★中で何が起きているかの説明（溝の底・胴の底・中心ポールのくぼみ 等）は
     落としてある。画面から決められない値なので、読んでも操作が変わらない。 */

/* プレビューの操作説明。3つとも .clickable が付くので、まわせるのは共通 */
export const PRESS_NOTE = '押すとカチッと沈む。半透明にすると中が見える。つかむとまわせる';

/* 押しきった形で作ってあること＋スイッチの部屋の大きさ */
export function travelNote(travelMM, roomSideMM, subject = 'モデル') {
  return `押しきった形で作っている。指を離すと${subject}が ${travelMM.toFixed(1)}mm 上がる。`
       + `スイッチの部屋は ${roomSideMM}mm角で自動`;
}

/* 十字穴のついた柱。寸法はスイッチの実測で決まるので、いじれない */
export const BOSS_NOTE = '太さ・長さは実測で決まる（変えられない）';
